import React from "react";

const Foot = (props) => {
    return(
        <footer className = "footer">"Be yourself; everyone else is already taken." Oscar Wilde.</footer>
    )
}

export default Foot;